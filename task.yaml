---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

params:
  git-repo-name: default-repo-name
  MESSAGE: default-message

inputs:
  - name: ((git-repo-name))

run:
  path: echo
  args: [((MESSAGE))); ls]
